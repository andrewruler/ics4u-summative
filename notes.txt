Task Notes:

- make data structures immutable
- improve css

Current task:
- genres

Learning Notes:
- useRef is useState but no rerender
- function takes in a callback function, runs it and uses its result

Questions to ask:
- Should read from firestore in order to see purchased movies?
- is it ok to use uuid for email
- is it ok to use split for storing bio
- should we make selectedGenres immutable
- how to handle genres with google signin

- when to read from firestore